function make_shirt(message, size) {
    if (size === void 0) { size = "Large"; }
    console.log("Your ".concat(size, " size shirt with the message ").concat(message, " is ready."));
}
make_shirt('I love Typescrit');
