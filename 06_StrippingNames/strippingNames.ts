let myName:string=' \tMuhammad Ali   \t';
console.log(`This is the text with some white spaces : ${myName}`)
let strippedName:string=myName.trim()
console.log(`Now the text is stripped off : ${strippedName}`)